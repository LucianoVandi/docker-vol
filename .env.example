# Symfony environment
APP_ENV=dev
BACKUP_TIMEOUT=300
APP_DEBUG=1
# Helper container used for volume tar/du operations and Docker fallback validation.
# Pinning keeps behavior reproducible; update deliberately for security patches.
DOCKERVOL_HELPER_IMAGE=alpine:3.20
#DOCKER_BACKUP_DEV_MODE=1
#HOST_PROJECT_DIR=
#CONTAINER_PROJECT_DIR=/app
